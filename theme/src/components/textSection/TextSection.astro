---
interface Props {
  bg?: string;
  txt?: string;
  mxw?: string;
}
const { bg = "", txt, mxw } = Astro.props as Props;
---

<div
  class={`mt-8 text-left ${bg && `bg-[${bg}] text-[${txt}]`} ${
    mxw && `${mxw}`
  } `}
>
  <slot />
</div>
