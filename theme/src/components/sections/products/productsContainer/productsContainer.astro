---
import ProductCard from '../productCard/productCard.astro'
---

<section class="grid-container sm:mx-auto sm:w-11/12 ">
  {
    [...Array(12)].map(el => <ProductCard/>)
  }
  <!-- Repeat the above book-card elements for more books -->
</section>

<!-- more products -->

<style>
  .grid-container {
    max-width: 1024px;
    display: grid;
    grid-gap: 20px;
    padding: 10px;
    grid-template-columns: repeat(auto-fill, minmax(300px, auto));
  }

</style>
